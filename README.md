# Arowana Staking

Modularized MasterChef Staking Pool with ERC-4626 LP tokens

Try running some of the following tasks:

```shell
yarn compile
yarn lint --fix
yarn build
yarn docs:dev
yarn test
yarn coverage
```
